/**
 * Checks if the value is either null ro undefined
 * @param {*} value
 * @returns {Boolean}
 */
function isNullOrUndefined(value) {
    return (typeof value === 'undefined' || value === null);
}
