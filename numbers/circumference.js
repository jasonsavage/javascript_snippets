/**
 * Calculates the circumference of a circle based on it's radius.
 * 
 * @param {number} radius
 * @returns {number} - circumference
 */
function circumference (radius) {
	return Math.PI * (radius *  2);
}
